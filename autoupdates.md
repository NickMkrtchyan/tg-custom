один простой плагин, и ты хочешь:

делать изменения локально

пушить в GitHub

и чтобы сайт сам обновлял плагин с GitHub

Это легче всего сделать с помощью Plugin Update Checker (PUC) — 2 минуты работы.

✅ Самый простой и рабочий вариант
Используем plugin-update-checker и GitHub Releases

👉 Это именно то, что нужно, чтобы твой сайт автоматом "увидел" новую версию.

🟢 ШАГ 1 — Скачай библиотеку

Скачай сюда:
https://github.com/YahnisElsts/plugin-update-checker

Положи в свой плагин:

/wp-content/plugins/your-plugin/
    plugin.php
    plugin-update-checker/

🟢 ШАГ 2 — Подключи в своём плагине

Вставь в plugin.php:

require plugin_dir_path(__FILE__) . 'plugin-update-checker/plugin-update-checker.php';

$updateChecker = Puc_v4_Factory::buildUpdateChecker(
    'https://github.com/USERNAME/REPO_NAME/',
    __FILE__,
    'your-plugin-slug'
);

$updateChecker->setBranch('main');

// Если используешь GitHub релизы:
$updateChecker->getVcsApi()->enableReleaseAssets();

🟢 ШАГ 3 — На GitHub создай теги (версии)

Когда пушишь на GitHub:

Сделай релиз:

GitHub → Releases

Create new release

Tag: v1.0.1

Upload ZIP плагина (опционально, но лучше)

WordPress увидит:

новую версию

скачает ZIP

предложит Update now

🟢 ШАГ 4 — Обновление на сайте работает автоматически!

❗ После пуша + создания релиза:

В WP:

Plugins → "Update available"
или сразу по крону.

Работает стабильно.

🔵 ⚠️ Важно

PUC НЕ обновляет автоматически при каждом пуше.
Он обновляет только если:

есть новый tag на GitHub

или новая релиз версия (v1.0.1 → v1.0.2)

То есть всегда обновляешь так:
git commit
git push origin main
git tag v1.0.1
git push origin v1.0.1

🟣 Если хочешь обновления "каждый пуш"

Когда не нужен релиз — есть способ:

Можно сделать обновление по ветке (branch):
$updateChecker->setBranch('main');


И тогда обновление будет подтягиваться:

➡ по коммитам
➡ без релизов
➡ но обязательно обновляй номер версии в plugin.php:

Version: 1.0.3


WordPress сравнивает именно это.