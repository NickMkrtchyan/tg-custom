# Диагностика проблемы "Мои курсы"

## Проблема
Кнопка "Мои курсы" не реагирует на нажатие в Telegram боте.

## Возможные причины

### 1. Несовпадение текста кнопки
Самая частая причина - текст на кнопке не совпадает с текстом в настройках.

**Как проверить:**
1. Зайдите в WordPress админку
2. Перейдите в **TG Course Bot → Localization**
3. Найдите поле **"Кнопка: Мои курсы"** (`tgcb_btn_my_courses`)
4. Проверьте, что там написано **точно** `👤 Мои курсы` (с эмодзи и пробелом)

### 2. Меню не обновилось
Telegram кэширует клавиатуру. Если вы изменили текст кнопки, старое меню может остаться.

**Как исправить:**
1. Отправьте боту команду `/start`
2. Бот должен отправить новое меню с обновленными кнопками

### 3. Проверка логов
Я добавил debug логирование в код (версия 1.0.4).

**Как проверить:**
1. Обновите плагин до версии 1.0.4
2. Нажмите кнопку "Мои курсы" в Telegram
3. Проверьте лог-файл WordPress (обычно `wp-content/debug.log`)
4. Найдите строку вида:
   ```
   TGCB Button Click: "👤 Мои курсы" | Expected My Courses: "👤 Мои курсы"
   ```
5. Если тексты **не совпадают** - это и есть проблема

## Решение

### Вариант 1: Сбросить настройки кнопок
1. Зайдите в **TG Course Bot → Localization**
2. Найдите секцию **"Кнопки меню"**
3. Убедитесь, что значения такие:
   - **Кнопка: Все курсы** = `📚 Все курсы`
   - **Кнопка: Мои курсы** = `👤 Мои курсы`
   - **Кнопка: Помощь** = `❓ Помощь`
   - **Кнопка: Поддержка** = `👨‍💻 Поддержка`
4. Нажмите **"Сохранить изменения"**
5. Отправьте боту `/start` в Telegram

### Вариант 2: Проверить базу данных
Если проблема сохраняется, проверьте значение в базе данных:

```sql
SELECT option_value FROM wp_options WHERE option_name = 'tgcb_btn_my_courses';
```

Должно быть: `👤 Мои курсы`

### Вариант 3: Обновить принудительно
Выполните в phpMyAdmin или через wp-cli:

```sql
UPDATE wp_options SET option_value = '👤 Мои курсы' WHERE option_name = 'tgcb_btn_my_courses';
```

Затем отправьте `/start` боту.

## Проверка работы

После исправления:
1. Отправьте боту `/start`
2. Нажмите кнопку "👤 Мои курсы"
3. Бот должен ответить сообщением о ваших курсах (или сообщением "Вы еще не записались ни на один курс")

## Если ничего не помогло

Проверьте лог-файл после обновления до версии 1.0.4 и пришлите мне строку из лога. Это поможет точно определить проблему.
